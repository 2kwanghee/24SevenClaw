# {{ project_name }} — Development Guide

> 이 파일은 ClickEye로 자동 생성되었습니다. ({{ generated_at }})
> Claude Code가 이 프로젝트에서 작업할 때 참조하는 루트 가이드입니다.

## Project Overview

- **프로젝트**: {{ project_name }}
- **유형**: {{ project_type }}
{% if stack %}
- **백엔드**: {{ stack.backend }}
- **프론트엔드**: {{ stack.frontend }}
{% endif %}
{% if catalog_entry %}
- **아키텍처 패턴**: {{ catalog_entry.get('architecture_pattern', '') }}
{% endif %}

## Agent Files

각 모듈 개발 시 해당 에이전트 파일을 참조:

{% for ref in agent_refs %}
- `{{ ref.file }}` — {{ ref.name }}
{% endfor %}

{% if catalog_entry and (catalog_entry.get('design_philosophy') or catalog_entry.get('implementation_constraints')) %}
## 프로젝트 구현 방식

{% if catalog_entry.get('design_philosophy') %}
**설계 철학**:
{{ catalog_entry.get('design_philosophy') }}

{% endif %}
{% if catalog_entry.get('implementation_constraints') %}
**구현 제약사항**:
{% for constraint in catalog_entry.get('implementation_constraints', []) %}
- {{ constraint }}
{% endfor %}

{% endif %}
{% if catalog_entry.get('tech_stack_tags') %}
**권장 기술 스택**: {{ catalog_entry.get('tech_stack_tags', []) | join(', ') }}

{% endif %}
{% if catalog_entry.get('recommended_agents') %}
**권장 에이전트**: {{ catalog_entry.get('recommended_agents', []) | join(', ') }}
{% endif %}
{% if catalog_entry.get('excluded_agents') %}
**제외 에이전트** (이 프로젝트에 불필요): {{ catalog_entry.get('excluded_agents', []) | join(', ') }}
{% endif %}
{% endif %}

## 하네스 엔지니어링

이 프로젝트는 하네스 엔지니어링이 적용되어 있습니다.
모든 코드 작업은 Router → Context → Loop → Worker 4단계를 거칩니다.
상세: `.claude/agents/harness-guide.md`

## Directory Structure

| 역할 | 디렉토리 |
|------|---------|
| 프론트엔드 코드 | `front/` |
| 백엔드 코드 | `backend/` |

> **주의**: 프레임워크명·프로젝트명 기반 디렉토리명 사용 금지.
> 올바른 예: `front/`, `backend/`
> 잘못된 예: `clickeye-web/`, `myapp/`, `next-app/`, `react-app/`

## Development Rules

1. **에이전트 가이드 참조**: 작업 전 해당 모듈의 agent.md를 반드시 읽을 것
2. **검증 필수**: 코드 변경 후 lint + typecheck + test 통과 확인
3. **절대 경로 사용**: 모든 파일 참조는 절대 경로
4. **한국어 커밋/주석**: 커밋 메시지와 주석은 한국어로 작성
5. **디렉토리 명명**: 프론트엔드는 `front/`, 백엔드는 `backend/` 사용 (프레임워크명 금지)

## Conventions

- **브랜치**: `feature/{module}/{description}`, `fix/{module}/{description}`
- **커밋**: `[module] 작업 내용`
- **테스트**: 새 기능은 반드시 테스트 동반

{% if stack %}
## 검증 명령어

```bash
# 백엔드
{{ stack.lint.backend }}
{{ stack.typecheck.backend }}
{{ stack.test.backend }}

# 프론트엔드
{{ stack.lint.frontend }}
{{ stack.typecheck.frontend }}
{{ stack.test.frontend }}
```
{% endif %}
