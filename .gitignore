# === Node ===
node_modules/

# === 파생형 하네스 워크스페이스(조달된 남의 레포 — 추적 금지) ===
workspaces/

# === 파생형 하네스 Tier 3a 메트릭 원장(런타임 수집물 — 추적 금지) ===
logs/metrics/

# === Flow-Ops 파이프라인 런타임 ===
.ralph/tasks/
.ralph/PLAN.md
.ralph/TASK.md
.ralph/fix_plan.md
.ralph/.pipeline_lock
.ralph/.pipeline_lock.*
.ralph/.task_mapping.json
.ralph/.task_mapping_full.json
.ralph/.iteration_count
.ralph/.pipeline_result.json
.ralph/.task_list.tmp
# 시트 풀 원장/자격증명 (CE-345 — 계정 배정과 OAuth 토큰은 절대 추적하지 않는다)
.ralph/seats.json
.ralph/seats/
# 워크스페이스 매핑 원장 — 머신마다 자기 머신 API 를 폴링해 만드는 호스트 로컬 상태이며
# 고객 repo URL 이 담긴다. seats.json 과 같은 취급(docs/multiproject-delivery.md §5-3).
# 추적하면 자율 파이프라인의 자동커밋이 고객 repo 주소를 커밋한다.
.ralph/workspaces.json
.ralph/.seat_lock.*
# 디스패처 런타임(러너 PID 마커·틱 락) — 호스트 로컬 상태 (CE-346)
.ralph/dispatch/
# llm-prompt-evolve 배치 후보(전이 산출물 — 챔피언/ledger 만 추적)
clickeye-llm/prompts/candidates/
.worktrees/
logs/
.env
.mcp.json
__pycache__/
.claude/current-plan.md

# === Obsidian (개인용 지식관리 레이어) ===
.obsidian/
obsidian/

# === HUD 세션 상태 (세션 로컬 — 추적 시 매 턴 자동커밋 노이즈 + 브랜치 전환 충돌) ===
# .omc 전체가 아니라 state/ 만 제외 — 향후 공유 설정은 추적 가능하게 남긴다.
.omc/state/

# === 런타임 PID 파일 (호스트 로컬 — 머신마다 다르고 매 기동마다 바뀐다) ===
# webhook-doctor.sh / fullstack_run.sh 가 .run/*.pid 를 쓴다. 추적하면 자율 파이프라인의
# 자동커밋(FLOWOPS_AUTO_COMMIT)이 이 파일을 "WIP" 로 커밋해 main 을 오염시킨다(실측
# 2026-08-04: `WIP(root): ngrok PID 파일 추가` 커밋이 로컬 main 을 분기시켜 PR 머지 후
# fast-forward 가 불가능해졌다).
.run/

# === Next.js 빌드 산출물 (레포 루트에 새는 경우) ===
# clickeye-web/.gitignore 가 자기 디렉토리의 .next/ 는 막지만, `npx next build` 를 레포
# 루트 cwd 에서 돌리면 텔레메트리 trace 가 루트에 .next/ 를 만든다. Stop 훅 자동커밋이
# 이걸 집어가므로 루트에서도 막는다.
.next/
