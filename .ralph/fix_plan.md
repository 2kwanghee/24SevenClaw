# Ralph Loop — 작업 큐 (Fix Plan)

> Claude가 이 파일을 읽고 미완료(`- [ ]`) 항목을 처리한다.
> 완료 시 `- [x]`로 표시하고 커밋한다.
> `- [!]`는 건너뛴 항목 (사유 기록 필수).

---

## P1: 기능 요구사항

- [x] **[web] 에러 바운더리 + 전역 토스트 시스템 구축**
  > 요청사항: ## 목표

API 에러, 렌더링 에러를 사용자에게 일관되게 알려주는 시스템을 구축한다.

## 현황

* React Error Boundary 미구현 (error.tsx, not-found.tsx 부재)
* API 에러 시 개별 페이지에서 AlertCircle로만 표시, 토스트 없음
* 스켈레톤 UI 없음 (로딩 중 빈 화면)

## 작업 내용

* `app/error.tsx` — 글로벌 에러 페이지
* `app/not-found.tsx` — 404 페이지
* 토스트 시스템 도입 (sonner 또는 react-hot-toast)
* API 에러 → 자동 토스트 표시
* 프로젝트 목록 스켈레톤 UI 추가

## 사이즈: S

---

## 진행 로그

> Ralph가 작업하면서 여기에 기록을 남긴다.

| 시각 | 항목 | 상태 | 비고 |
|------|------|------|------|