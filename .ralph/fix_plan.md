# Ralph Loop — 작업 큐 (Fix Plan)

> Claude가 이 파일을 읽고 미완료(`- [ ]`) 항목을 처리한다.
> 완료 시 `- [x]`로 표시하고 커밋한다.
> `- [!]`는 건너뛴 항목 (사유 기록 필수).

---

## P1: 기능 요구사항

- [!] **[P0-2][spike] 구독 시트 기반 동시성 산정 (데스크탑 러너 주력)**
  > **2026-07-29 — 산출물 확정 후 큐에서 제외.** 산출물(동시성 예산 메모)은
  > `docs/hybrid-runner-headless-poc.md` §4에 작성됨.
  >
  > **제외 사유 (에이전트가 완료할 수 없는 부분):**
  > 1. **전제 1건 폐기** — "구독 세션당 동시 `claude -p` N개 실측"은 P4가 확정한
  >    *사용자당 시트 1개(ToS 방어)* 와 충돌한다. 한 계정의 동시 세션을 늘리는 실험은
  >    설계 방향과 반대이므로 수행하지 않았다. 동시성 단위는 `시트 = 슬롯 1개`로 확정(§4-1).
  > 2. **사람 액션 2건** — ⓐ 구독 시트 수는 API/CLI 조회 경로가 없어 Anthropic Console에서
  >    사람이 확인해야 한다. ⓑ 실토큰 다계정·레이트 N배 재실증은 팀원 시트가 등록되어야
  >    가능하다(현재 실토큰 1개).
  > 3. **입력 미확정 1건** — 필요 시트 수의 유일한 미지수는 MVP 동시 실행 목표 `C`이며
  >    `migration.md` I-12 인터뷰 응답 대상이다. 임의 기본값을 넣지 않았다.
  >
  > **확정된 결론:** 병렬의 병목은 시트가 아니라 **러너 호스트 수용량과 git main 머지
  > 경로**다(P5 실측: 단일 체크아웃 병렬 배제). 현행 유효 동시성 M=1. 시트를 늘려도
  > 러너 수평 확장 전에는 동시성이 늘지 않는다. §3 리소스 거버넌스의 *구조*는
  > `1 시트 = 1 슬롯`으로 확정되어 더 이상 블로킹 상태가 아니다.
  >
  > 잔여 사람 액션은 `.ralph` 큐가 아니라 `docs/hybrid-runner-headless-poc.md` §4-6에서 추적한다.
  >
  > 원 요청사항: ## Phase 0 — 토대 · de-risking (SPIKE · 2026-07-15 재정의)

## 목표

실행을 **[claude.ai](<http://claude.ai>) 구독 OAuth 세션(구독 시트)** 주력으로 확정하고, 그 위에서 동시에 몇 개의 AI 개발 세션을 안정적으로 돌릴 수 있는지 산정한다. 조직 API 키(종량제 크레딧) 경로는 **최소 폴백으로 격하**.

## 재정의 사유

크레딧(종량제 API) 사용을 지양하고 예측 가능한 구독 시트로 개발한다는 방침 확정 — 고정가 SI 딜리버리 경제성에 맞음. 따라서 하이브리드의 무게추를 데스크탑 러너(구독 시트)로 옮긴다. 기존 조직-API-키 컨테이너 PoC 결과(인증 경로는 유효, 크레딧 부족으로 실응답 미검증)는 `docs/hybrid-runner-headless-poc.md`에 보존 — 폴백 참고용.

## 작업

* 현재 확보된 [claude.ai](<http://claude.ai>) 구독 플랜/시트 수 확인 (Anthropic Console — 사람 액션)
* 구독 세션(`ANTHROPIC_API_KEY` unset)으로 동시 `claude -p` 헤드리스 세션을 N개 띄웠을 때 안정적으로 도는 상한 실측 (**크레딧 불필요 — 구독 세션으로 측정**)
* 사용량 창(usage window)·레이트 제약이 동시성에 주는 영향 파악
* "시트 N개 → 동시 러너 M개" 매핑 산정

## 완료 기준 (산출물 = 동시성 예산 메모)

- 구독 플랜/시트 수 확인
- 구독 세션당 안정적 동시 헤드리스 세션 수 실측
- 시트→동시 러너 매핑 + 20~50 프로젝트 목표 대비 필요 시트 수 산정
- 데스크탑 러너 주력 설계 확정 (조직 API 키 팔은 폴백으로 격하)

## 영향

P3 데스크탑 러너 스케줄 + §3 리소스 거버넌스(구독 시트 기반 동시성 세마포어)를 확정. 조직 API 키 팔은 최소 폴백으로만 유지.

---

## 진행 로그

> Ralph가 작업하면서 여기에 기록을 남긴다.

| 시각 | 항목 | 상태 | 비고 |
|------|------|------|------|
| 2026-07-29 | [P0-2][spike] 구독 시트 동시성 산정 | `[!]` 제외 | 산출물 = `docs/hybrid-runner-headless-poc.md` §4. 전제 1건 폐기(ToS 충돌) · 사람 액션 2건 · I-12 입력 미확정. 확정: `1 시트 = 1 슬롯`, 병목은 러너 호스트·git 머지 |