## 목표
리허설 저장소(`24seven-delivery-rehearsal`)에 추가할 `docs/INSTALL.md`의 섹션 계약(제목·순서·사실 범위)과 대상 저장소 사실관계를 확정하여, 후속 구현 티켓이 창작 없이 그대로 작성만 하도록 한다.

## 가정 (Assumptions)
- 인테이크 원문의 '리허설 저장소'는 워크스페이스 매핑(`.ralph/workspaces.json`, `workspace_key: 71e00c25`, `intake_id: 71e00c25-b32a-4838-909e-c1ae448969e3`)이 가리키는 `https://github.com/2kwanghee/24seven-delivery-rehearsal.git`이며, 로컬 클론 경로는 `/mnt/c/workspace/ClickEye/workspaces/71e00c25`다. ClickEye 본 저장소는 폴백으로 사용하지 않는다(작업 내용 1 준수).
- 기존 문서 프론트매터 관례가 대상 저장소에 없으므로(아래 §조사 결과), `docs/INSTALL.md`에 프론트매터를 추가하지 않는다 — 없는 관례를 창작하지 않는 것이 가장 보수적인 선택.
- 브랜치 규약 인용은 ClickEye 루트 `CLAUDE.md`의 `{type}/{module}/{TICKET-KEY}-{description}` 형식을 그대로 인용하되, 대상 저장소는 module 개념이 없는 단일 저장소이므로 예시에서 `{module}` 자리는 저장소명 또는 생략 가능함을 명시한다(과잉 창작 금지, 원문 컨벤션 문자열만 인용).

## 조사 결과 (대상 저장소 사실관계)
- 대상 저장소 절대 경로: `/mnt/c/workspace/ClickEye/workspaces/71e00c25` (원격 `origin` = `https://github.com/2kwanghee/24seven-delivery-rehearsal.git`, 기본 브랜치 `main` — `.clickeye_default_branch` 확인).
- `README.md` 존재: **있음** — 2줄짜리 최소 문서(`# 24seven-delivery-rehearsal` + 동일 텍스트 1줄). 프론트매터 없음.
- `docs/` 디렉토리 존재: **없음**.
- 기존 문서 프론트매터 관례: **없음**(README에 프론트매터 없고 docs/ 자체가 없어 참조할 관례 부재).

## 대상 파일
- `docs/INSTALL.md` (신규, 대상 저장소 `/mnt/c/workspace/ClickEye/workspaces/71e00c25/docs/INSTALL.md`) — 아래 섹션 계약대로 신규 작성. `docs/` 디렉토리도 함께 신규 생성 필요.

## 구현 단계 (본 티켓 = 계약 확정만, 실제 파일 작성은 후속 티켓)
1. 문서 제목: `# 24seven-delivery-rehearsal 설치 안내` (저장소명 인용, 창작 금지).
2. `## 사전 요구사항` — `git` 설치 여부만 기술. 런타임(Node/Python 등) 언급 금지.
3. `## 클론 방법` — `git clone https://github.com/2kwanghee/24seven-delivery-rehearsal.git` 명령 + `cd 24seven-delivery-rehearsal` 디렉토리 진입만 기술.
4. `## 브랜치 규약` — ClickEye 루트 `CLAUDE.md`의 `{type}/{module}/{TICKET-KEY}-{description}` 컨벤션 원문 인용 + 예시 1~2개(예: `fix/CE-360-install-doc`, `feature/CE-361-...`; 대상 저장소는 단일 저장소이므로 `{module}` 세그먼트는 생략 가능함을 1줄로 명시).

섹션 순서는 위 1→4 순서로 고정하며, 후속 구현 티켓은 이 섹션 제목·순서·사실 범위를 그대로 따르고 추가 판단을 하지 않는다.

## 테스트
- 코드 변경이 아닌 문서 구조 확정 티켓이므로 자동화 테스트 대상 없음.
- 검증 방법: 후속 구현 티켓 완료 후 `docs/INSTALL.md`가 위 3개 섹션·순서·사실 범위와 정확히 일치하는지 육안 대조.

## 준수할 컨벤션
- 루트 `CLAUDE.md`: 절대 경로 사용, 브랜치 규약 `{type}/{module}/{TICKET-KEY}-{description}`.
- `docs/README.md` 매니페스트 규약은 **ClickEye 본 저장소 문서에만 적용**되며 대상 저장소(`24seven-delivery-rehearsal`)에는 프론트매터 관례가 부재하므로 강제하지 않는다(가정 참조).

## 제외 범위
- 빌드·배포·환경변수·의존성 설치 안내
- README.md 재구성
- CI 워크플로·PR/이슈 템플릿 신설
- `docs/INSTALL.md` 실제 파일 작성(본 티켓은 계약 확정까지만; 작성은 후속 구현 티켓)
