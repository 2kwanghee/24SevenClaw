Now I have enough context. Producing the refined implementation spec.

## 목표
Ralph 파이프라인 이터레이션 시작 시 이전(실패) 런이 남긴 `.ralph/TASK.md`·`.ralph/REVIEW.md`·`.ralph/review.stream.jsonl`을 무조건 정리해, `[ ! -f .ralph/TASK.md ]` 가드가 낡은 메타데이터를 재사용해 QA 리뷰어에게 전달하는 오염을 차단한다.

## 가정 (Assumptions)
- `.ralph/` 산출물은 `IMPL_WORKDIR`(고객 워크스페이스)가 아니라 항상 `PROJECT_DIR/.ralph`에 생성된다 — 코드 전수 확인: `.ralph/TASK.md`·`.ralph/REVIEW.md`·`.ralph/review.stream.jsonl` 참조 8곳 모두 `$IMPL_WORKDIR` 접두 없이 상대경로 사용(`scripts/auto_dev_pipeline.sh:381,456,1210,1233,1242,1261,1396,1637`). 워크스페이스 딜리버리(WS_DELIVERY) 여부와 무관하게 단일 위치.
- "이터레이션 시작 시"는 `while true` 루프 내에서 `ISSUE_KEY`/`BRANCH`가 확정되는 지점(`auto_dev_pipeline.sh:576`) 직후로 정한다 — 이보다 이르면 아직 처리할 태스크가 있는지 모르고(가드 라인 551~564에서 `break`), 이보다 늦으면 이후 실패 `continue` 경로 중 일부(예: 1266, 1279행 QA 판정 실패/실행불능 분기)가 정리 없이 `continue`하는 지점을 놓친다.
- "실패 런이 남긴 잔재"는 성공 경로(1396, 1637행)가 이미 정리하는 것과 별개로, **이전 이터레이션에서 `continue`로 빠져나가며 정리하지 않은 파일**을 가리킨다. 가장 보수적인 방법은 성공/실패 경로 분기를 추적하지 않고 매 이터레이션 시작 시 무조건 3개 파일을 삭제하는 것 — 이유: 어느 실패 경로가 정리를 빠뜨렸는지 전수 추적하는 것보다, 시작 시점 무조건 삭제가 회귀 위험이 낮고 성공 경로 cleanup과 상충하지 않는다(멱등 `rm -f`).
- `.ralph/PLAN.md`는 이 티켓 범위 밖(요청 대상은 TASK.md·REVIEW.md·review.stream.jsonl 3개로 명시됨) — 삭제 목록에 추가하지 않는다.

## 대상 파일
- `scripts/auto_dev_pipeline.sh` — `while true` 루프 내 `ISSUE_KEY`/`BRANCH` 확정 직후(576행 `IFS='|' read ...` 다음 줄)에 이전 런 잔재 정리 블록 추가.

## 구현 단계
1. `scripts/auto_dev_pipeline.sh:576` (`IFS='|' read -r ISSUE_KEY ISSUE_ID BRANCH TASK_MODE TITLE <<< "$TASK_INFO"`) 바로 다음 줄에 아래 블록 삽입:
   ```bash
   # ── [CE-399] 이전(실패) 런 잔재 정리 — 태스크 픽업 직후, 성공 경로 cleanup(1396/1637행)과 무관하게
   # 이번 이터레이션 시작 시 무조건 제거한다. 실패 런이 남긴 TASK.md 를 [ ! -f ] 가드가 재사용해
   # 낡은 누적 변경 파일 목록을 QA 리뷰어에게 전달 → 델타 diff 와 모순 → 판정불가 반복(CE-398 후속).
   rm -f .ralph/TASK.md .ralph/REVIEW.md .ralph/review.stream.jsonl
   ```
2. 기존 `[ ! -f .ralph/TASK.md ]` 가드(1210행)는 수정하지 않는다 — 위 정리로 인해 이 가드는 매 이터레이션마다 자연히 "항상 재생성"과 동일하게 동작하게 된다(가드 로직 자체는 유지, 금지범위인 QA 판정 로직·델타 기준과 무관).
3. 성공 경로 cleanup(1396행, 1637행)은 그대로 둔다 — 중복 삭제라도 `rm -f`는 멱등이라 부작용 없음.

## 테스트
- 이 스크립트는 기존 shell 단위 테스트가 없는 운영 파이프라인이므로, 정적 검증으로 대체:
  - `bash -n scripts/auto_dev_pipeline.sh`로 문법 오류 없음 확인.
  - 수동 시나리오: `.ralph/TASK.md`에 임의 mtime/내용을 만든 뒤 파이프라인 1회 이터레이션을 트리거해 `ISSUE_KEY` 확정 직후 해당 파일이 삭제(또는 재생성으로 mtime 갱신)되는지 로그로 확인.
- 수용 기준 2번째 항목("판정불가 오염 재발 0")은 SIP 재실행 실전 검증 — 코드 리뷰/유닛테스트로 커버되지 않는 운영 검증이므로, 구현 완료 후 별도로 SIP-2 재실행을 통해 확인 필요(이 스펙의 구현 범위 밖, 사용자/운영자 후속 작업으로 명시).

## 준수할 컨벤션
- 커밋 메시지 한국어, `[root]` 또는 해당 모듈 프리픽스 (`.claude/CLAUDE.md` — 이 변경은 파이프라인 스크립트이므로 `[root]` 사용, CE-398 후속 커밋들과 동일 관례).
- 변경 이유를 코드 주석에 근거(선행 사고·재발 방지 맥락)와 함께 남기는 기존 스타일 준수(예: 1216~1218행, 1244~1247행 패턴 — "왜"를 주석에 남김).
- 최소 수정(T2: 조건문·정리 로직 추가) — 구조 변경 없음, 가드 자체는 유지.

## 도메인 제약 (Domain Constraints)
- **정합성 규칙**: `.ralph/TASK.md`·`.ralph/REVIEW.md`·`.ralph/review.stream.jsonl`은 이터레이션 스코프 산출물이며, 이전 이터레이션의 값이 다음 이터레이션의 QA 입력으로 새어 들어가서는 안 된다(CE-398/CE-399 계열 불변식). 새 산출물류를 추가할 때도 이 규칙을 따라야 한다.
