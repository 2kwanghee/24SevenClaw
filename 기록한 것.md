기록한 것

  1. 잘못된 정보 정정 — 이게 가장 중요하다. 제가 여러 턴에 걸쳐 "infraeye-harness는 리뷰 미결(우회 7계열·비밀정보 미탐 43%)"이라고 보고했지만 7-27 스냅샷을 계속
  인용한 오류였다. 실측 결과:

  S0·S1 모두 REVIEW_APPROVED (리뷰어 2인 정족수 + 공격자 조건 충족)
  테스트 241 pass / 0 fail
  우회 7계열 → F1~F7 계열별 커버리지 테스트로 승격
  미탐 43%  → S2 계열 편입 처리
  잔여 미충족 = install.sh 부재 (엔진 아니라 설치 배관)

  문서·LoadMap·Linear 티켓 전부에 정정을 박아 뒀다.

  2. P8 방식 확정 (docs §8-1) — 흡수 vs 재구현이 아니라 층 분리로 결론냈다. 측정 근거: 엔진 5,756줄 중 우회 방어(gitguard 1,544 + secrets 593)는 프로젝트 중립이라
  이식 대상이고, worktree/shard 212회는 infraeye3 실행 모델 전용이라 이식 대상이 아니다(우리 축은 P4·P5에서 만든 프로젝트→시트→러너). 런타임은 근거가 못 된다 —
  node 21ms / python 16ms / 현행 bash 훅 30ms로 지금 것이 가장 느리다.

  3. 후속 백로그 (docs §8-2 + Linear)

  ┌────────┬───────────────────────────────────────────────────────────────┬──────────────────────────────────────────────────────────────────────┐
  │ Linear │                             과제                              │                                  값                                  │
  ├────────┼───────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────┤
  │ CE-328 │ F-1 사용량 인제스트                                           │ P4 모니터링이 지금 절반만 작동 — 핵심가치 직결. 시트별 레이트의 선행 │
  ├────────┼───────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────┤
  │ CE-329 │ F-3 P8 집행면                                                 │ T3 티어 착수 시. 방식 확정됨                                         │
  ├────────┼───────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────┤
  │ CE-330 │ F-2 실토큰 실증 · F-4 gates 자동해석 · F-5 종량 스크립트 처분 │ 조건부                                                               │
  └────────┴───────────────────────────────────────────────────────────────┴──────────────────────────────────────────────────────────────────────┘

  현재 상태

  다프로젝트 전환 9/10 완료 (P8만 의도적 보류). 무인 체인은 ①수신→②정제→③발급→④완주→⑤정합성→⑥콜백 전 구간이 구현·E2E 실증된 상태다.

  미푸시 커밋이 상당히 쌓였다 — 다음 작업 전에 ! git push origin main 한 번 실행해 두시는 걸 권한다. 그리고 dev DB 컨테이너(clickeye-db/redis)는 P4 검증 때 띄운 뒤
  그대로 살려뒀다 — 다음 작업에 쓰실 것 같으면 두고, 아니면 내려도 된다.